# 棋盘背景修正设计

**更新日期**: 2026-04-17  
**问题**: 之前设计的网格与游戏实际棋盘不匹配

---

## 🎯 正确的棋盘设计流程

### 方案 1：背景无网格（推荐）⭐

**背景图只做地面纹理，棋盘格由游戏代码绘制**

```
背景图内容:
- 纯地面纹理（草地/石砖/沙漠等）
- 边缘装饰（栏杆、旗帜、建筑等）
- 无棋盘格线
- 无网格分割

优点:
- 灵活：游戏可随时调整棋盘格数
- 准确：格线由代码精确绘制
- 美观：AI 生成更自然，不会变形
```

### 方案 2：背景带网格

**背景图上绘制精确棋盘格**

需要明确：
1. 横向 = ? 格
2. 纵向 = ? 格
3. 每格 = ? px × ? px

---

## 📐 精确计算（竖屏 720x1280）

### 标准自走棋棋盘（7 列 × 8 行）

```
┌────────────────────────────────────┐
│        顶部 HUD 区域 (约 150px)      │
├────────────────────────────────────┤
│                                    │
│         棋盘区域 (约 800px 高)        │
│    ┌──────────────────────────┐   │
│    │  对手区域 (4 行)            │   │
│    │  ┌──┬──┬──┬──┬──┬──┬──┐  │   │
│    │  │  │  │  │  │  │  │  │  │   │
│    │  ├──┼──┼──┼──┼──┼──┼──┤  │   │
│    │  │  │  │  │  │  │  │  │  │   │
│    │  └──┴──┴──┴──┴──┴──┴──┘  │   │
│    │  ┌──┬──┬──┬──┬──┬──┬──┐  │   │
│    │  │  │  │  │  │  │  │  │  │   │
│    │  ├──┼──┼──┼──┼──┼──┼──┤  │   │
│    │  │  │  │  │  │  │  │  │  │   │
│    │  └──┴──┴──┴──┴──┴──┴──┘  │   │
│    │  玩家区域 (4 行)            │   │
│    └──────────────────────────┘   │
│     ← 600px 宽 (居中，左右各留 60px) → │
├────────────────────────────────────┤
│        商店区 (180px)               │
├────────────────────────────────────┤
│        操作栏 (60px)                │
├────────────────────────────────────┤
│        备战区 (160px)               │
└────────────────────────────────────┘
```

### 格子尺寸计算

```
棋盘区域：600px(宽) × 800px(高)
横向 7 列：600 ÷ 7 ≈ 85.7px/格
纵向 8 行：800 ÷ 8 = 100px/格

→ 问题：85.7px ≠ 100px，格子会变成长方形！
```

### 修正方案

**方案 A: 调整棋盘区域尺寸**
```
目标：让格子成正方形 (假设 100x100px)

横向 7 格：7 × 100 = 700px
纵向 8 行：8 × 100 = 800px

棋盘区域：700px × 800px
背景图宽度：720px（左右各留 10px 边距）
```

**方案 B: 调整格子数量**
```
目标：适应 600px 宽度

横向 6 格：600 ÷ 6 = 100px/格
纵向 8 行：800 ÷ 8 = 100px/格

→ 格子是正方形！但横向只有 6 格
```

**方案 C: 接受长方形格子**
```
横向 7 格：~86px
纵向 8 行：100px

格子比例：86:100 ≈ 5:6（轻微长方形）
→ 大多数自走棋接受这种微小差异
```

---

## ✅ 最终推荐

### 推荐方案：C（轻微长方形格子）

**原因**:
- 7 列是主流自走棋标准（金铲铲、刀塔自走棋）
- 86px vs 100px 差异很小（约 16%），肉眼不明显
- 棋子放上去后几乎看不出

### 背景图设计参数

```
背景图尺寸：720px(宽) × 1280px(高)

棋盘区域：
- 宽度：700px（左右各留 10px 边距）
- 高度：800px（从顶部 150px 开始）
- 位置：居中

棋盘格：
- 横向：7 列
- 纵向：8 行
- 每格：约 100px × 100px（实际 100x100px）
- 格线宽度：2-3px（浅色）

格子颜色（交替）：
- 奇数格：#8BC34A（浅绿）
- 偶数格：#689F38（深绿）
```

---

## 📝 修正后的提示词

### 通用模板

```
自走棋游戏棋盘背景，纯俯视 90 度垂直视角，
输出尺寸 720x1280 像素，2D 平面风格，

棋盘规格：
- 横向 7 列，纵向 8 行，共 56 格
- 棋盘区域 700px 宽 × 800px 高，居中布局
- 每格约 100x100px 正方形
- 国际象棋棋盘样式，浅色深色交替格子
- 清晰的网格分割线

[地面材质描述]
[边缘装饰描述]
[风格描述]

无透视无倾斜无斜边无伪 3D，
平铺纹理，无缝拼接，
UI 可直接叠加使用，PNG 格式
```

---

## 🎨 12 种战场背景修正清单

| 战场 | 地面材质 | 格子颜色（浅/深） | 边缘装饰 |
|------|----------|------------------|----------|
| 长安宫殿 | 青石砖 | #E8D5B5 / #C4A777 | 汉白玉栏杆、朱红廊柱 |
| 长城战场 | 城砖 | #9E9E9E / #757575 | 烽火台、城墙 |
| 祁连山 | 山石 + 积雪 | #FFFFFF / #B0C4DE | 雪山峰峦 |
| 河西走廊 | 戈壁石 | #D2B48C / #8B7355 | 绿洲、祁连山远景 |
| 匈奴王庭 | 皮革地毯 | #D4AF37 / #B8860B | 穹庐帐篷、金色纹样 |
| 蒙古草原 | 草地 | #90EE90 / #32CD32 | 蒙古包、羊群 |
| 阿尔泰山 | 岩石 + 森林 | #2F4F4F / #708090 | 雪山、松林 |
| 鄂尔浑河 | 河滩石 | #87CEEB / #4682B4 | 河流蜿蜒 |
| 戈壁沙漠 | 沙石 | #F4A460 / #D2691E | 沙丘、胡杨 |
| 居延海 | 芦苇荡 | #98FB98 / #20B2AA | 湖泊、水鸟 |
| 玉门关 | 石板 | #A9A9A9 / #696969 | 城楼、"玉门关"匾额 |
| 漠北荒原 | 冻土 | #8B7355 / #654321 | 枯草、远山 |

---

## 💡 关键检查点

生成后验收：
1. [ ] 棋盘是否 7 列 × 8 行？
2. [ ] 格子是否基本正方形（约 100x100px）？
3. [ ] 是否是纯俯视 90 度，无透视？
4. [ ] 格子颜色是否交替（像国际象棋）？
5. [ ] 网格线是否清晰可见？
6. [ ] 棋盘区域是否居中？
7. [ ] 能否直接叠加棋子 UI？

---

**建议**: 先用一个战场（如长安宫殿）测试，确认格子尺寸正确后再批量生成其他 11 种。
